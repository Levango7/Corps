# =============================================================================
# Corps 部署环境变量模板
# 用法：cp .env.example .env，然后填入真实值
# 生成强随机密钥：openssl rand -hex 32
# =============================================================================

# ─── PostgreSQL 数据库 ──────────────────────────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=cde5c8ed4f42f7bf880d0e46
POSTGRES_DB=corps

# ─── 应用数据库连接 URL ─────────────────────────────────────────────────────
# 注意：docker-compose 中 app 容器通过 service name 'db' 连接，host 为 db
# 本地直连时 host 为 localhost
DATABASE_URL=postgresql://postgres:cde5c8ed4f42f7bf880d0e46@db:5432/corps?schema=public

# ─── 认证密钥（生产必须换为强随机值）─────────────────────────────────────────
# 生成命令：openssl rand -hex 32
JWT_ACCESS_SECRET=replace-with-secure-random-string-32-hex
JWT_REFRESH_SECRET=replace-with-secure-random-string-32-hex
BETTER_AUTH_SECRET=replace-with-secure-random-string-32-hex

# ─── 应用 URL ───────────────────────────────────────────────────────────────
# 生产环境改为实际域名，如 https://corps.example.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Stripe（可选，未配置时计费页隐藏升级入口）──────────────────────────────
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# ─── 邮件服务（可选，Resend）─────────────────────────────────────────────────
RESEND_API_KEY=
EMAIL_FROM=

# ─── RLS 运行时角色密码（可选，仅 RLS 加固模式使用）─────────────────────────
# 与 db/schema.sql 中应用 RLS 时设置的 corps_app 角色密码一致
CORPS_APP_PASSWORD=