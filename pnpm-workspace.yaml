packages:
  - web
  - desktop

allowBuilds:
  - '@prisma/client'
  - '@prisma/engines'
  - better-sqlite3
  - esbuild
  - prisma
# 强制升级 sharp 到 >=0.35.4 修复 GHSA-rgj7-g3m4-5g8c（libheif 漏洞）
# sharp 是 next 的间接依赖，next 16.3.3 默认拉取 <0.35.4 的版本
overrides:
  sharp: ">=0.35.4"
